# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
# ... outras configurações ...
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4 # ou v3, v2
      #- run: npm ci && npm run build # <--- ESTA LINHA PRECISA SER REMOVIDA OU COMENTADA
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_GESTAO_DE_LANCAMENTOS }}'
          channelId: live # ou ${{ github.event.pull_request.head.ref }} para PRs
          projectId: gestao-de-lancamentos
# ...